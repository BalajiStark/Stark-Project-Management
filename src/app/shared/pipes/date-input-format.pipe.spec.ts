import { DateInputFormatPipe } from './date-input-format.pipe';

describe('DateInputFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInputFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
